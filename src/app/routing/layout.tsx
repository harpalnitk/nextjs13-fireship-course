export default function RoutingLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
    <div>
        <h1>Layout of routing routes</h1>
        {children}
    </div>
    )
  }